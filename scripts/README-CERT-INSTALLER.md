# Cloudflare Certificate Installer

Автоматический скрипт для установки Cloudflare Origin Certificate.

## Что делает скрипт

✅ Создает директорию `/etc/xray/certs/`
✅ Запрашивает сертификат и ключ (интерактивно)
✅ Проверяет валидность сертификата
✅ Устанавливает правильные права доступа
✅ Обновляет docker-compose.yaml (опционально)
✅ Перезапускает Docker контейнер (опционально)
✅ Проверяет доступность сертификатов в контейнере
✅ Сохраняет информацию о сертификате

## Использование

### Запуск

```bash
cd /root/xray-cloudflare-setup/scripts
sudo ./install-cloudflare-cert.sh
```

### Процесс установки

1. **Запрос домена**
   ```
   Введите ваш основной домен (например: myfly.space):
   > myfly.space
   ```

2. **Инструкция по получению сертификата**
   - Скрипт покажет пошаговую инструкцию
   - Откройте Cloudflare Dashboard параллельно

3. **Вставка Origin Certificate**
   ```
   Вставьте Origin Certificate из Cloudflare:
   (весь блок от -----BEGIN CERTIFICATE----- до -----END CERTIFICATE-----)

   Вставьте сертификат и нажмите Enter, затем Ctrl+D:
   ```

   **Как вставить:**
   - Скопируйте весь блок из Cloudflare (включая BEGIN/END)
   - Вставьте в терминал
   - Нажмите Enter
   - Нажмите Ctrl+D (для завершения многострочного ввода)

4. **Вставка Private Key**
   ```
   Вставьте Private Key из Cloudflare:
   (весь блок от -----BEGIN PRIVATE KEY----- до -----END PRIVATE KEY-----)
   ```

   Повторите процесс для ключа.

5. **Автоматическая настройка**
   - Скрипт проверит валидность
   - Установит права доступа
   - Предложит обновить docker-compose.yaml
   - Предложит перезапустить контейнер

6. **Готово!**
   - Сертификаты установлены
   - Показаны следующие шаги

## Примеры

### Полный процесс

```bash
$ sudo ./install-cloudflare-cert.sh

============================================
Cloudflare Origin Certificate Installer
============================================

[INFO] Этот скрипт установит Cloudflare Origin Certificate для Xray
[INFO] Сертификат будет работать для всех субдоменов (wildcard)

[INFO] Введите ваш основной домен (например: myfly.space):
> myfly.space
[✓] Домен: myfly.space

[INFO] Создание директории для сертификатов...
[✓] Директория создана: /etc/xray/certs

============================================
Инструкция: Получение сертификата в Cloudflare
============================================

Если вы еще не создали сертификат:

1. Откройте: https://dash.cloudflare.com/
2. Выберите ваш домен: myfly.space
3. SSL/TLS → Origin Server
4. Нажмите: Create Certificate
...

Нажмите Enter когда будете готовы вставить сертификат...

============================================
Шаг 1: Origin Certificate
============================================

Вставьте Origin Certificate из Cloudflare:
...

[вставляете сертификат, Enter, Ctrl+D]

[✓] Сертификат сохранен: /etc/xray/certs/cloudflare-origin.pem

============================================
Шаг 2: Private Key
============================================

[вставляете ключ, Enter, Ctrl+D]

[✓] Ключ сохранен: /etc/xray/certs/cloudflare-origin-key.pem
[✓] Права доступа установлены
[✓] Сертификат валиден!

[INFO] Информация о сертификате:
  Subject: CN = *.myfly.space
  Valid until: Jan 10 12:34:56 2040 GMT

[✓] Найден docker-compose: /root/docker-compose.yaml
[!] Volume для сертификатов НЕ настроен в docker-compose

Добавить автоматически? (yes/no): yes
[✓] Volume добавлен в docker-compose.yaml

Перезапустить Docker контейнер? (yes/no): yes
[INFO] Перезапуск Docker контейнера...
[✓] Контейнер перезапущен
[✓] Сертификаты видны в контейнере!

============================================
Установка завершена!
============================================
```

## После установки

### 1. Пути в конфиге панели

Используйте эти пути (внутри контейнера):

```json
"tlsSettings": {
  "certificates": [{
    "certificateFile": "/etc/xray/certs/cloudflare-origin.pem",
    "keyFile": "/etc/xray/certs/cloudflare-origin-key.pem"
  }]
}
```

### 2. Cloudflare настройки

- **DNS:** A запись для субдоменов → IP сервера
- **Proxy:** ON (🟠 оранжевое облако)
- **SSL/TLS Mode:** Full (strict)
- **Network → WebSockets:** ON

### 3. Для разных субдоменов

Просто меняйте `Host`:

```json
// cdn.myfly.space
"headers": { "Host": "cdn.myfly.space" }

// cdn2.myfly.space
"headers": { "Host": "cdn2.myfly.space" }
```

Сертификат один и тот же! Работает для всех `*.myfly.space`

## Файлы после установки

```
/etc/xray/certs/
├── cloudflare-origin.pem        # Сертификат (644)
├── cloudflare-origin-key.pem    # Ключ (600)
└── cert-info.txt                # Информация о сертификате
```

## Проверка установки

### Проверить файлы

```bash
ls -lh /etc/xray/certs/
```

Должно быть:
```
-rw-r--r-- 1 root root 1.2K Nov 10 15:00 cloudflare-origin.pem
-rw------- 1 root root 1.7K Nov 10 15:00 cloudflare-origin-key.pem
-rw-r--r-- 1 root root  567 Nov 10 15:00 cert-info.txt
```

### Проверить валидность

```bash
openssl x509 -in /etc/xray/certs/cloudflare-origin.pem -noout -text | grep -A 2 "Subject:"
```

Должно показать:
```
Subject: CN = *.myfly.space
```

### Проверить в контейнере

```bash
docker exec -it remnanode ls -la /etc/xray/certs/
```

Должны видеть те же файлы.

## Повторная установка

Если нужно заменить сертификат:

```bash
sudo ./install-cloudflare-cert.sh
```

При обнаружении существующих сертификатов:
```
[!] Обнаружены существующие сертификаты!
Перезаписать? (yes/no): yes
[✓] Резервная копия создана
```

Старые сертификаты сохраняются как:
- `cloudflare-origin.pem.backup-20251110-150000`
- `cloudflare-origin-key.pem.backup-20251110-150000`

## Troubleshooting

### Ошибка: "Неверный формат"

**Причина:** Неполный блок сертификата/ключа

**Решение:** Убедитесь что копируете ВЕСЬ блок:
```
-----BEGIN CERTIFICATE-----
MIIEpDCCAowCCQC...
(все строки)
...8X4Zw==
-----END CERTIFICATE-----
```

### Ошибка: "Сертификат невалиден"

**Причина:** Поврежденный сертификат при вставке

**Решение:**
1. Запустите скрипт заново
2. Скопируйте сертификат более аккуратно
3. Проверьте что нет лишних пробелов/символов

### Docker контейнер не видит сертификаты

**Причина:** Volume не добавлен или контейнер не перезапущен

**Решение:**
```bash
# Проверить docker-compose.yaml
grep "xray/certs" docker-compose.yaml

# Если нет, добавить:
nano docker-compose.yaml

volumes:
  - /etc/xray/certs:/etc/xray/certs:ro

# Перезапустить
docker-compose down && docker-compose up -d
```

## Обновление docker-compose вручную

Если скрипт не смог автоматически обновить:

```yaml
services:
  remnanode:
    volumes:
      - /var/lib/remnanode/xray:/usr/local/bin/xray
      - /var/lib/remnanode/geoip.dat:/usr/local/share/xray/geoip.dat
      - /var/lib/remnanode/geosite.dat:/usr/local/share/xray/geosite.dat
      - /etc/xray/certs:/etc/xray/certs:ro  # ← ДОБАВЬТЕ ЭТУ СТРОКУ!
```

## Информация о сертификате

После установки вся информация сохранена:

```bash
cat /etc/xray/certs/cert-info.txt
```

Содержит:
- Домен
- Дата установки
- Пути к файлам
- Детали сертификата
- Примеры использования
- Настройки Cloudflare

## Безопасность

✅ Сертификат (публичный) - права 644 (читать можно)
✅ Ключ (приватный) - права 600 (только root)
✅ Владелец: root:root
✅ Резервные копии при перезаписи
✅ Read-only mount в Docker (:ro)

## Удаление

Если нужно удалить сертификаты:

```bash
rm -rf /etc/xray/certs/
```

Перезапустите Docker если использовался volume.

---

**Автор:** Claude Code
**Версия:** 1.0
**Дата:** 2025-11-10
