# Интеграция с панелью управления Remnawave/Marzban

## Введение

Это руководство объясняет как адаптировать наш WebSocket через Cloudflare конфиг для работы с панелью управления (Remnawave, Marzban, 3x-ui и подобные).

**Ключевое отличие:**
- **Чистый Xray:** Вы вручную прописываете клиентов в `config.json`
- **Панельный Xray:** Панель динамически управляет клиентами через API

---

## Основные отличия панельного конфига

### 1. Пустой массив clients

**Чистый конфиг:**
```json
"clients": [
  {
    "id": "4ffe2244-47fc-40f1-914c-d1b39158dbf3",
    "email": "user1@example.com",
    "level": 0
  }
]
```

**Панельный конфиг:**
```json
"clients": []
```

**Почему пустой:**
- Панель динамически добавляет/удаляет клиентов через Xray API
- Не нужно редактировать конфиг вручную
- Все управление через веб-интерфейс панели

---

### 2. Tag для inbound

**Обязательно указать:**
```json
{
  "tag": "TM-CDN-WS-IN",   // ← Уникальный тег для панели
  "port": 443,
  "protocol": "vless"
}
```

**Зачем:**
- Панель идентифицирует inbound по тегу
- При создании пользователя панель указывает на какой inbound его добавить
- Можно иметь несколько inbound'ов с разными тегами

---

### 3. Явное указание listen

**Панельный конфиг:**
```json
{
  "listen": "0.0.0.0",  // ← Явно указываем слушать на всех интерфейсах
  "port": 443
}
```

**Зачем:**
- Панель может работать на разных интерфейсах
- Явное указание предотвращает ошибки

---

### 4. Расширенный routing

**Панельный конфиг обычно включает:**
```json
"routing": {
  "rules": [
    {
      "type": "field",
      "ip": ["geoip:private"],
      "outboundTag": "BLOCK"          // Блокируем private IP
    },
    {
      "type": "field",
      "domain": ["geosite:private"],
      "outboundTag": "BLOCK"          // Блокируем private домены
    },
    {
      "type": "field",
      "protocol": ["bittorrent"],
      "outboundTag": "BLOCK"          // Блокируем BitTorrent
    }
  ]
}
```

**Зачем:**
- Безопасность (блокируем доступ к внутренним ресурсам)
- Экономия трафика (блокируем торренты)
- Панель может добавлять свои правила

---

## Структура панельного конфига для WebSocket

### Полный конфиг: remnawave-websocket.json

```json
{
  "log": {
    "loglevel": "warning"
  },
  "inbounds": [
    {
      "tag": "TM-CDN-WS-IN",           // ← Уникальный тег
      "port": 443,
      "listen": "0.0.0.0",             // ← Явно указываем интерфейс
      "protocol": "vless",
      "settings": {
        "clients": [],                 // ← ПУСТОЙ! Панель управляет
        "decryption": "none"
      },
      "sniffing": {
        "enabled": true,
        "destOverride": [
          "http",
          "tls",
          "quic"
        ]
      },
      "streamSettings": {
        "network": "ws",               // ← WebSocket
        "security": "tls",
        "tlsSettings": {
          "certificates": [
            {
              "certificateFile": "/etc/xray/certs/cert.pem",
              "keyFile": "/etc/xray/certs/key.pem"
            }
          ],
          "alpn": [
            "h2",
            "http/1.1"
          ],
          "minVersion": "1.2"
        },
        "wsSettings": {
          "path": "/ws-path",          // ← WebSocket path
          "headers": {
            "Host": "cdn.myfly.space"  // ← Ваш домен
          },
          "maxEarlyData": 2048,
          "earlyDataHeaderName": "Sec-WebSocket-Protocol"
        }
      }
    }
  ],
  "outbounds": [
    {
      "tag": "DIRECT",
      "protocol": "freedom"
    },
    {
      "tag": "BLOCK",
      "protocol": "blackhole"
    }
  ],
  "routing": {
    "domainStrategy": "IPIfNonMatch",
    "rules": [
      {
        "type": "field",
        "ip": ["geoip:private"],
        "outboundTag": "BLOCK"
      },
      {
        "type": "field",
        "domain": ["geosite:private"],
        "outboundTag": "BLOCK"
      },
      {
        "type": "field",
        "protocol": ["bittorrent"],
        "outboundTag": "BLOCK"
      }
    ]
  }
}
```

---

## Интеграция с панелью Remnawave/Marzban

### Шаг 1: Подготовка сервера

#### 1.1. TLS сертификаты

Панель обычно ожидает сертификаты в определенных путях.

**Вариант A: Использовать существующие пути**

Если панель уже настроена, узнайте где она хранит сертификаты:

```bash
# Найти конфиг панели
find /opt /var /etc -name "xray*.json" 2>/dev/null

# Посмотреть пути к сертификатам
grep -r "certificateFile" /opt/marzban/ 2>/dev/null
```

**Вариант B: Настроить стандартные пути**

Скопируйте сертификаты куда удобно панели:

```bash
# Создайте директорию
mkdir -p /etc/xray/certs

# Скопируйте сертификаты
cp /etc/letsencrypt/live/cdn.myfly.space/fullchain.pem /etc/xray/certs/cert.pem
cp /etc/letsencrypt/live/cdn.myfly.space/privkey.pem /etc/xray/certs/key.pem

# Права доступа (важно для панели!)
chown -R marzban:marzban /etc/xray/certs/  # Если панель работает под пользователем marzban
# ИЛИ
chown -R xray:xray /etc/xray/certs/        # Если под пользователем xray

chmod 644 /etc/xray/certs/cert.pem
chmod 600 /etc/xray/certs/key.pem
```

---

### Шаг 2: Добавление WebSocket inbound в панель

#### Для Marzban:

**2.1. Через веб-интерфейс:**

1. Откройте панель: `https://your-panel-domain:8000`
2. Войдите как администратор
3. **Settings → Xray Configuration**
4. Найдите секцию `inbounds`
5. Добавьте новый inbound из файла `remnawave-websocket.json`

**2.2. Через конфигурационный файл:**

```bash
# Найдите конфиг панели
nano /var/lib/marzban/xray_config.json

# Добавьте inbound из remnawave-websocket.json в массив inbounds
# Сохраните (Ctrl+O, Enter, Ctrl+X)

# Перезапустите панель
systemctl restart marzban
```

---

#### Для 3x-ui:

**Через веб-интерфейс:**

1. Откройте панель: `https://your-server-ip:2053`
2. **Inbounds → Add Inbound**
3. **Settings:**
   - Protocol: VLESS
   - Port: 443
   - Network: WebSocket
   - Path: `/ws-path`
   - TLS: Enable
   - Certificate File: `/etc/xray/certs/cert.pem`
   - Key File: `/etc/xray/certs/key.pem`
4. **Save**

---

### Шаг 3: Создание пользователей через панель

После добавления inbound'а:

1. **Users → Add User**
2. **Settings:**
   - Username: user1
   - Protocol: VLESS
   - **Inbound Tag:** `TM-CDN-WS-IN` ← Выберите ваш новый inbound
   - Traffic Limit: (опционально)
   - Expiry Date: (опционально)
3. **Save**

**Панель автоматически:**
- Сгенерирует UUID
- Добавит клиента в `clients: []` через Xray API
- Создаст vless:// ссылку
- Покажет QR код

---

### Шаг 4: Проверка работы

#### 4.1. Проверить что inbound добавлен

```bash
# Посмотреть активный конфиг Xray
curl http://localhost:8080/api/stats/system 2>/dev/null | jq '.inbounds'

# Или через логи панели
journalctl -u marzban -n 50
```

#### 4.2. Проверить что клиенты добавляются

```bash
# Создайте тестового пользователя через панель

# Проверьте в логах Xray
journalctl -u xray -n 20

# Должны видеть:
# "Added user: UUID to inbound: TM-CDN-WS-IN"
```

#### 4.3. Тест подключения

1. Скопируйте vless:// ссылку из панели
2. Импортируйте в клиент (V2rayNG, V2rayN)
3. Подключитесь
4. Проверьте логи:

```bash
tail -f /var/log/xray/access.log

# Должны видеть активность от нового пользователя
```

---

## Сосуществование Reality и WebSocket

Вы можете иметь **оба inbound'а одновременно**:

```json
{
  "inbounds": [
    {
      "tag": "REALITY-IN",      // ← Существующий Reality
      "port": 443,
      "protocol": "vless",
      "streamSettings": {
        "network": "raw",
        "security": "reality"
      }
    },
    {
      "tag": "TM-CDN-WS-IN",    // ← Новый WebSocket для ТМ
      "port": 8443,             // ← ДРУГОЙ ПОРТ!
      "protocol": "vless",
      "streamSettings": {
        "network": "ws",
        "security": "tls"
      }
    }
  ]
}
```

**Важно:**
- Reality на порту 443 (основной)
- WebSocket на порту 8443 (для Туркменистана)
- В Cloudflare настройте проксирование на порт 8443

**Cloudflare Origin Rule:**
1. **Rules → Origin Rules → Create Rule**
2. **Match:**
   - Hostname equals `cdn.myfly.space`
3. **Then:**
   - Destination Port: `8443`
4. **Deploy**

Теперь:
- `gr-1.myfly.space:443` → Reality (прямое подключение)
- `cdn.myfly.space:443` → Cloudflare → 8443 → WebSocket (для ТМ)

---

## Отличия от чистого Xray

| Параметр | Чистый Xray | Панельный Xray |
|----------|-------------|----------------|
| **Клиенты** | Вручную в `config.json` | Панель управляет через API |
| **UUID** | Генерируете сами | Панель генерирует |
| **Добавление юзеров** | Редактировать конфиг + restart | Через веб-интерфейс, без restart |
| **vless ссылки** | Генерируете вручную | Панель создает автоматически |
| **Статистика** | Нет | Панель показывает трафик |
| **Лимиты** | Нет | Панель может ставить лимиты |
| **Управление** | SSH + nano | Веб-интерфейс |

---

## Управление пользователями в панели

### Создание пользователя для Туркменистана

1. **Users → Add User**
2. **Username:** `tm_user1`
3. **Inbound Tag:** `TM-CDN-WS-IN` ← Выберите WebSocket inbound
4. **Traffic Limit:** 50 GB (опционально)
5. **Expiry Date:** 30 days (опционально)
6. **Save**

Панель вернет:
```
vless://AUTO-GENERATED-UUID@cdn.myfly.space:443?type=ws&path=/ws-path&host=cdn.myfly.space&security=tls#tm_user1
```

**Отправьте эту ссылку пользователю!**

---

### Статистика и мониторинг

**Через панель вы увидите:**
- 📊 Использованный трафик по каждому пользователю
- 📈 Активные подключения
- 📅 Дата последнего подключения
- ⏰ Время до истечения
- 🚫 Возможность заблокировать пользователя

---

## Обновление домена в панели

Если меняете домен (при блокировке):

### Метод 1: Через веб-интерфейс

1. **Settings → Xray Configuration**
2. Найдите `wsSettings.headers.Host`
3. Измените на новый домен: `cdn2.myfly.space`
4. Найдите `tlsSettings.certificates`
5. Обновите пути к новым сертификатам
6. **Save & Restart**

### Метод 2: Через конфиг-файл

```bash
# Откройте конфиг панели
nano /var/lib/marzban/xray_config.json

# Найдите и замените:
# "Host": "cdn.myfly.space"  →  "Host": "cdn2.myfly.space"
# Обновите пути к сертификатам

# Перезапустите
systemctl restart marzban
```

**Панель автоматически:**
- Обновит все vless:// ссылки существующих пользователей
- Новые ссылки будут с новым доменом

---

## Troubleshooting

### Проблема 1: Панель не видит новый inbound

**Решение:**
```bash
# Проверьте логи панели
journalctl -u marzban -n 50

# Проверьте что Xray запущен
systemctl status xray

# Перезапустите панель
systemctl restart marzban
```

---

### Проблема 2: Клиенты не добавляются

**Причины:**
- Xray API не доступен
- Неправильный tag
- Конфликт портов

**Решение:**
```bash
# Проверьте что Xray API работает
curl http://localhost:8080/stats/system

# Если не работает, добавьте в конфиг:
{
  "api": {
    "tag": "api",
    "services": [
      "HandlerService",
      "StatsService"
    ]
  },
  "stats": {},
  "policy": {
    "system": {
      "statsInboundUplink": true,
      "statsInboundDownlink": true
    }
  }
}
```

---

### Проблема 3: Сертификаты не читаются

**Причины:**
- Неправильные права доступа
- Панель работает под другим пользователем

**Решение:**
```bash
# Узнайте под каким пользователем работает панель
ps aux | grep marzban

# Дайте права этому пользователю
chown -R USERNAME:USERNAME /etc/xray/certs/
chmod 644 /etc/xray/certs/cert.pem
chmod 600 /etc/xray/certs/key.pem
```

---

## Best Practices

### 1. Разделение inbound'ов по регионам

```json
{
  "inbounds": [
    {
      "tag": "REALITY-MAIN",     // Reality для стабильных регионов
      "port": 443
    },
    {
      "tag": "WS-TURKMENISTAN",  // WebSocket для ТМ через Cloudflare
      "port": 8443
    },
    {
      "tag": "WS-IRAN",          // WebSocket для Ирана
      "port": 8444
    }
  ]
}
```

**Преимущества:**
- Разные домены для разных регионов
- Изолированное управление
- Блокировка одного не затрагивает другие

### 2. Именование тегов

Используйте описательные теги:
```
GOOD: "TM-CDN-WS-IN", "REALITY-GERMANY-IN"
BAD:  "IN1", "IN2", "VLESS"
```

### 3. Регулярные бэкапы конфига панели

```bash
# Создайте cron задачу для бэкапа
crontab -e

# Добавьте:
0 3 * * * cp /var/lib/marzban/xray_config.json /root/backups/xray_config.$(date +\%Y\%m\%d).json
```

---

## Миграция с чистого Xray на панель

Если у вас уже работает чистый Xray и вы хотите перейти на панель:

### Шаг 1: Установите панель

```bash
# Пример для Marzban
sudo bash -c "$(curl -sL https://github.com/Gozargah/Marzban-scripts/raw/master/marzban.sh)" @ install
```

### Шаг 2: Импортируйте существующую конфигурацию

```bash
# Скопируйте ваш текущий конфиг
cp /usr/local/etc/xray/config.json /var/lib/marzban/xray_config.json

# Измените:
# 1. "clients": [...] → "clients": []
# 2. Добавьте "tag" для каждого inbound
# 3. Добавьте "listen": "0.0.0.0"
```

### Шаг 3: Создайте пользователей в панели

Для каждого существующего UUID:
1. Создайте пользователя в панели с **тем же UUID**
2. Выберите соответствующий inbound
3. Старые ссылки продолжат работать!

---

## Заключение

**Панельный подход:**
- ✅ Удобнее управлять пользователями
- ✅ Статистика и лимиты
- ✅ Не нужно редактировать конфиг вручную
- ✅ Автоматическая генерация ссылок

**Интеграция WebSocket через Cloudflare:**
- ✅ Работает с панелями
- ✅ Защита IP сервера сохраняется
- ✅ Быстрая смена домена
- ✅ Совместимо с Reality на других портах

---

**Следующие шаги:**
1. Добавьте WebSocket inbound в вашу панель
2. Создайте тестового пользователя
3. Проверьте подключение
4. Настройте Cloudflare (если еще не настроено)
5. Раздайте ссылки пользователям в ТМ

**Конфиг готов:** `/root/xray-cloudflare-setup/configs/panel/remnawave-websocket.json`

---

**Дата создания:** 2025-11-10
