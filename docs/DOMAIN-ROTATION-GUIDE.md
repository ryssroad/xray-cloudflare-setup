# Руководство по ротации доменов

## Зачем нужна ротация доменов?

При блокировке домена в Туркменистане (или другой стране с цензурой) вы можете **быстро переключиться на резервный домен** без изменения сервера или его IP адреса.

**Преимущества:**
- ⏱️ Смена домена занимает 5-10 минут
- 🔒 Реальный IP сервера остается скрытым
- 🚀 Минимальный даунтайм для пользователей
- 💰 Не нужен новый сервер

---

## Стратегия: Горячий резерв

Подготовьте 3-4 домена заранее, чтобы при блокировке мгновенно переключиться.

### Схема:

```
Основной:    cdn.myfly.space     [АКТИВЕН]
Резерв 1:    cdn2.myfly.space    [ГОТОВ]
Резерв 2:    cdn3.myfly.space    [ГОТОВ]
Резерв 3:    backup.example.com  [ГОТОВ]
```

Все домены:
- ✅ Указывают на один IP сервера
- ✅ Настроены в Cloudflare (Proxy: ON)
- ✅ Имеют валидные TLS сертификаты
- ✅ Готовы к использованию

---

## Подготовка резервных доменов

### Шаг 1: Создайте DNS записи в Cloudflare

**Cloudflare Dashboard → DNS → Records:**

```
Type  Name                Value           Proxy
A     cdn                 ВАШ_IP          🟠 Proxied
A     cdn2                ВАШ_IP          🟠 Proxied
A     cdn3                ВАШ_IP          🟠 Proxied
A     backup              ВАШ_IP          🟠 Proxied
```

**Важно:** Все с включенным Proxy (оранжевое облако)!

---

### Шаг 2: Получите сертификаты для всех доменов

#### Вариант A: Let's Encrypt (бесплатно, но нужно обновлять каждые 90 дней)

```bash
# Остановите Xray временно (certbot нужен порт 80)
systemctl stop xray

# Получите сертификаты для всех доменов
certbot certonly --standalone \
  -d cdn.myfly.space \
  -d cdn2.myfly.space \
  -d cdn3.myfly.space \
  --non-interactive --agree-tos -m your-email@example.com

# Скопируйте сертификаты в /etc/xray/certs/
mkdir -p /etc/xray/certs/{cdn,cdn2,cdn3}

cp /etc/letsencrypt/live/cdn.myfly.space/fullchain.pem /etc/xray/certs/cdn/cert.pem
cp /etc/letsencrypt/live/cdn.myfly.space/privkey.pem /etc/xray/certs/cdn/key.pem

cp /etc/letsencrypt/live/cdn2.myfly.space/fullchain.pem /etc/xray/certs/cdn2/cert.pem
cp /etc/letsencrypt/live/cdn2.myfly.space/privkey.pem /etc/xray/certs/cdn2/key.pem

cp /etc/letsencrypt/live/cdn3.myfly.space/fullchain.pem /etc/xray/certs/cdn3/cert.pem
cp /etc/letsencrypt/live/cdn3.myfly.space/privkey.pem /etc/xray/certs/cdn3/key.pem

# Установите правильные права
chown -R nobody:nogroup /etc/xray/certs/
chmod 644 /etc/xray/certs/*/cert.pem
chmod 600 /etc/xray/certs/*/key.pem

# Запустите Xray
systemctl start xray
```

#### Вариант B: Cloudflare Origin Certificate (рекомендуется!)

**Преимущества:**
- ✅ Действует 15 лет
- ✅ Не нужно обновлять
- ✅ Работает только с Cloudflare (безопасно)
- ✅ Один сертификат для всех субдоменов (wildcard)

**Инструкция:**

1. **Cloudflare Dashboard → SSL/TLS → Origin Server**
2. **Create Certificate**
3. **Настройки:**
   - Certificate Type: Generate private key and CSR with Cloudflare
   - Hostnames: `*.myfly.space, myfly.space`
   - Certificate Validity: 15 years
   - Click **Create**

4. **Сохраните сертификат:**

```bash
# Создайте файл для сертификата
nano /etc/xray/certs/cloudflare-origin.pem
# Вставьте "Origin Certificate" из Cloudflare
# Сохраните (Ctrl+O, Enter, Ctrl+X)

# Создайте файл для ключа
nano /etc/xray/certs/cloudflare-origin-key.pem
# Вставьте "Private key" из Cloudflare
# Сохраните

# Права доступа
chown nobody:nogroup /etc/xray/certs/cloudflare-origin*.pem
chmod 644 /etc/xray/certs/cloudflare-origin.pem
chmod 600 /etc/xray/certs/cloudflare-origin-key.pem
```

**Преимущество:** Один сертификат работает для ВСЕХ субдоменов `*.myfly.space`!

---

### Шаг 3: Создайте конфигурации для резервных доменов

```bash
cd /root/xray-cloudflare-setup/configs/server

# Скопируйте текущую конфигурацию для резервных доменов
cp /usr/local/etc/xray/config.json config-cdn2.json
cp /usr/local/etc/xray/config.json config-cdn3.json

# Отредактируйте каждую:
# 1. Измените пути к сертификатам
# 2. Измените Host в wsSettings
```

**Пример для cdn2:**

```json
{
  "streamSettings": {
    "wsSettings": {
      "headers": {
        "Host": "cdn2.myfly.space"
      }
    },
    "tlsSettings": {
      "certificates": [{
        "certificateFile": "/etc/xray/certs/cdn2/cert.pem",
        "keyFile": "/etc/xray/certs/cdn2/key.pem"
      }]
    }
  }
}
```

**Или с Cloudflare Origin Certificate (один для всех):**

```json
{
  "streamSettings": {
    "wsSettings": {
      "headers": {
        "Host": "cdn2.myfly.space"
      }
    },
    "tlsSettings": {
      "certificates": [{
        "certificateFile": "/etc/xray/certs/cloudflare-origin.pem",
        "keyFile": "/etc/xray/certs/cloudflare-origin-key.pem"
      }]
    }
  }
}
```

---

## Быстрое переключение на резервный домен

### Когда cdn.myfly.space заблокирован:

**5 минут до восстановления работы!**

#### 1. Выберите резервный домен

```bash
# Проверьте что DNS работает
dig +short cdn2.myfly.space
# Должен вернуть Cloudflare IP
```

#### 2. Переключите конфигурацию

```bash
# Сделайте бэкап текущей
cp /usr/local/etc/xray/config.json /usr/local/etc/xray/config.json.backup-$(date +%Y%m%d)

# Используйте резервную
cp /root/xray-cloudflare-setup/configs/server/config-cdn2.json /usr/local/etc/xray/config.json

# Проверьте валидность
xray -test -config /usr/local/etc/xray/config.json

# Перезапустите
systemctl restart xray

# Проверьте статус
systemctl status xray
```

#### 3. Сгенерируйте новые vless ссылки

```bash
# Используйте скрипт (создадим ниже)
/root/xray-cloudflare-setup/scripts/generate-vless-links.sh cdn2.myfly.space

# Ссылки будут в /root/xray-client-configs/personal/
```

#### 4. Раздайте новые ссылки пользователям

**Отправьте каждому его новую ссылку:**
- User1: `vless://UUID1@cdn2.myfly.space:443?...`
- User2: `vless://UUID2@cdn2.myfly.space:443?...`
- User3: `vless://UUID3@cdn2.myfly.space:443?...`

**⏱️ Общее время: 5-10 минут!**

---

## Скрипт автоматической генерации vless ссылок

Создайте скрипт для быстрой генерации ссылок:

```bash
cat > /root/xray-cloudflare-setup/scripts/generate-vless-links.sh << 'SCRIPT'
#!/bin/bash

# Использование: ./generate-vless-links.sh DOMAIN
# Пример: ./generate-vless-links.sh cdn2.myfly.space

DOMAIN=$1

if [ -z "$DOMAIN" ]; then
  echo "Использование: $0 DOMAIN"
  echo "Пример: $0 cdn2.myfly.space"
  exit 1
fi

# UUID из конфига
UUID1="4ffe2244-47fc-40f1-914c-d1b39158dbf3"
UUID2="6be2f1ba-c689-4d23-8527-3ced307363e4"
UUID3="265c83c7-efa1-470d-be93-563bdba23113"

# Параметры WebSocket
TRANSPORT="ws"
PATH="/ws-path"
PORT="443"

# Генерация ссылок
LINK1="vless://${UUID1}@${DOMAIN}:${PORT}?encryption=none&security=tls&type=${TRANSPORT}&path=${PATH}&host=${DOMAIN}&sni=${DOMAIN}#User1-${DOMAIN}"
LINK2="vless://${UUID2}@${DOMAIN}:${PORT}?encryption=none&security=tls&type=${TRANSPORT}&path=${PATH}&host=${DOMAIN}&sni=${DOMAIN}#User2-${DOMAIN}"
LINK3="vless://${UUID3}@${DOMAIN}:${PORT}?encryption=none&security=tls&type=${TRANSPORT}&path=${PATH}&host=${DOMAIN}&sni=${DOMAIN}#User3-${DOMAIN}"

# Сохранение
mkdir -p /root/xray-client-configs/personal/${DOMAIN}

echo "$LINK1" > /root/xray-client-configs/personal/${DOMAIN}/user1-link.txt
echo "$LINK2" > /root/xray-client-configs/personal/${DOMAIN}/user2-link.txt
echo "$LINK3" > /root/xray-client-configs/personal/${DOMAIN}/user3-link.txt

# Вывод
echo "============================================"
echo "VLESS ссылки для домена: $DOMAIN"
echo "============================================"
echo ""
echo "User1:"
echo "$LINK1"
echo ""
echo "User2:"
echo "$LINK2"
echo ""
echo "User3:"
echo "$LINK3"
echo ""
echo "Ссылки сохранены в: /root/xray-client-configs/personal/${DOMAIN}/"
echo "============================================"

SCRIPT

chmod +x /root/xray-cloudflare-setup/scripts/generate-vless-links.sh
```

**Использование:**

```bash
/root/xray-cloudflare-setup/scripts/generate-vless-links.sh cdn2.myfly.space
```

---

## Мониторинг доменов

### Проверка доступности из разных регионов

Используйте сервисы:
- https://www.host-tracker.com/
- https://uptimerobot.com/
- https://ping.pe/

Настройте проверку каждые 5 минут для всех доменов.

### Cloudflare Analytics

**Dashboard → Analytics → Traffic:**
- Мониторьте трафик по домену
- Резкое падение = возможная блокировка

---

## Превентивные меры

### 1. Используйте разные типы доменов

```
cdn.myfly.space       ← Технический субдомен
files.myfly.space     ← Похож на файловый сервер
api.myfly.space       ← Похож на API
media.example.com     ← Совсем другой домен
```

Разные паттерны сложнее блокировать массово.

### 2. Периодически меняйте домены ЗАРАНЕЕ

Не ждите блокировки! Меняйте домен каждые 30-60 дней профилактически.

### 3. Не публикуйте домены публично

Не размещайте ваши VPN домены в:
- Публичных репозиториях GitHub
- Форумах
- Социальных сетях

Только личная рассылка пользователям.

---

## Checklist при смене домена

**Перед сменой:**
- [ ] Новый домен добавлен в Cloudflare DNS (Proxy: ON)
- [ ] TLS сертификат получен и установлен
- [ ] Конфигурация Xray подготовлена и протестирована
- [ ] vless ссылки сгенерированы

**При смене:**
- [ ] Остановить Xray
- [ ] Заменить конфигурацию
- [ ] Проверить конфигурацию: `xray -test`
- [ ] Запустить Xray
- [ ] Проверить статус: `systemctl status xray`
- [ ] Проверить логи: `journalctl -u xray -n 20`

**После смены:**
- [ ] Разослать новые ссылки всем пользователям
- [ ] Попросить подтвердить работу
- [ ] Мониторить логи на сервере: `tail -f /var/log/xray/access.log`
- [ ] Проверить Cloudflare Analytics через 1 час

---

## FAQ

**Q: Можно ли использовать несколько доменов одновременно?**
A: Да! Xray может слушать на одном порту (443) и роутить по Host header. Но проще использовать один активный домен.

**Q: Сколько стоят дополнительные домены?**
A: Субдомены (cdn2.myfly.space) - бесплатно. Новые домены - $10-15/год.

**Q: Нужно ли менять UUID при смене домена?**
A: Нет! UUID остаются теми же. Меняется только домен в ссылке.

**Q: Как понять что домен заблокирован?**
A:
- Пользователи не могут подключиться
- Логи на сервере пустые (нет подключений)
- DNS резолвится, но подключение не проходит

**Q: Что если заблокируют все субдомены myfly.space?**
A: Используйте совсем другой домен (example.com, test.org, etc.)

---

## Альтернативная стратегия: Domain Fronting

Если блокируют домены агрессивно, можно использовать **Domain Fronting** через Cloudflare:

**Принцип:**
```
SNI: www.microsoft.com           ← Отправляем в SNI
Host: cdn.myfly.space            ← Реальный хост в HTTP header
```

Cloudflare роутит по Host header, а цензор видит только SNI (microsoft.com).

**Минусы:**
- Cloudflare может это обнаружить и заблокировать
- Сложнее настроить
- Не все клиенты поддерживают

---

## Заключение

**Ваша архитектура с Cloudflare позволяет:**
- 🔄 Быстро менять домены (5-10 минут)
- 🔒 Держать реальный IP сервера скрытым
- 💪 Противостоять доменным блокировкам
- 📊 Мониторить ситуацию через Cloudflare

**Подготовьте 2-3 резервных домена заранее!**

При блокировке одного - мгновенно переключайтесь на другой. Сервер не трогаете, IP остается защищенным.

---

**Дата создания:** 2025-11-10
