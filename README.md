# Xray + Cloudflare CDN Setup

Автоматизированное решение для развертывания Xray с Cloudflare CDN для обхода блокировок в регионах с агрессивной цензурой.

## Описание проекта

Этот проект предназначен для тестирования эффективности проксирования VLESS через Cloudflare CDN для увеличения времени жизни VPN серверов в условиях DPI и блокировок.

### Цели проекта
- ⏱️ Увеличить время жизни серверов с ~30 дней до >60 дней
- 📊 Uptime >95%
- ⚡ Латентность <150ms дополнительной задержки от CDN
- 🚀 Минимальная деградация скорости

### Поддерживаемые транспорты
1. **gRPC** (приоритетный, рекомендуется для Cloudflare)
2. **WebSocket** (запасной вариант)
3. **HTTP/2** (дополнительное тестирование)

---

## Быстрый старт

### Требования
- Ubuntu 22.04+ или Debian 11+
- Права root
- Домен, зарегистрированный на Cloudflare
- Публичный IP адрес сервера

### Установка за 5 минут

```bash
# 1. Перейдите в директорию проекта
cd /root/xray-cloudflare-setup

# 2. Запустите установочный скрипт
./scripts/setup-server.sh

# 3. Следуйте инструкциям:
#    - Выберите транспорт (рекомендуется gRPC)
#    - Введите ваш домен
#    - Выберите метод получения TLS (рекомендуется Caddy)

# 4. Настройте Cloudflare (см. docs/cloudflare-setup.md)

# 5. Настройте мониторинг
./scripts/setup-monitoring.sh

# 6. Сгенерируйте клиентские конфигурации
./scripts/generate-client-config.sh
```

---

## Структура проекта

```
xray-cloudflare-setup/
├── configs/
│   ├── server/          # Серверные конфигурации
│   │   ├── config-grpc.json
│   │   ├── config-websocket.json
│   │   └── config-http2.json
│   └── client/          # Клиентские конфигурации (шаблоны)
│       ├── client-grpc.json
│       ├── client-websocket.json
│       └── client-http2.json
├── scripts/
│   ├── setup-server.sh          # Основной установочный скрипт
│   ├── switch-transport.sh      # Переключение между транспортами
│   ├── generate-client-config.sh # Генерация клиентских конфигов
│   ├── setup-monitoring.sh      # Настройка автоматического мониторинга
│   └── troubleshoot.sh          # Диагностика проблем
├── monitoring/
│   ├── check-xray.sh            # Проверка доступности
│   ├── collect-metrics.sh       # Сбор метрик
│   └── generate-report.sh       # Генерация отчетов
├── docs/
│   └── cloudflare-setup.md      # Подробная инструкция по Cloudflare
└── README.md                     # Этот файл
```

---

## Документация

### Основные руководства
1. [**Настройка Cloudflare**](docs/cloudflare-setup.md) - Подробная инструкция по настройке DNS, SSL/TLS и Network
2. [**Техническое задание**](../xray-tz.md) - Полное техническое задание проекта

### Скрипты и их назначение

#### Установка и настройка
- `setup-server.sh` - Автоматическая установка и настройка Xray сервера
  - Установка зависимостей и Xray
  - Настройка firewall (UFW)
  - Получение TLS сертификатов (Caddy/Certbot)
  - Применение конфигурации

#### Управление
- `switch-transport.sh` - Переключение между транспортами без переустановки
- `generate-client-config.sh` - Создание готовых клиентских конфигураций
- `troubleshoot.sh` - Автоматическая диагностика типичных проблем

#### Мониторинг
- `setup-monitoring.sh` - Настройка автоматических проверок через cron
- `check-xray.sh` - Проверка работоспособности (каждые 5 минут)
- `collect-metrics.sh` - Сбор метрик использования (каждый час)
- `generate-report.sh` - Генерация аналитических отчетов (еженедельно)

---

## Использование

### Первоначальная настройка

#### 1. Установка сервера
```bash
cd /root/xray-cloudflare-setup
./scripts/setup-server.sh
```

Скрипт автоматически:
- Проверит систему
- Установит зависимости
- Настроит firewall
- Получит TLS сертификаты
- Запустит Xray с выбранным транспортом

#### 2. Настройка Cloudflare
Следуйте инструкциям в [docs/cloudflare-setup.md](docs/cloudflare-setup.md)

Ключевые моменты:
- DNS запись с **Proxied** статусом (оранжевое облако)
- SSL/TLS mode: **Full (strict)**
- Network → gRPC: **On**
- Network → WebSockets: **On**

#### 3. Проверка работоспособности
```bash
./scripts/troubleshoot.sh
```

#### 4. Настройка мониторинга
```bash
./scripts/setup-monitoring.sh
```

#### 5. Получение клиентских конфигураций
```bash
./scripts/generate-client-config.sh
```

Готовые конфигурации будут в `/root/xray-client-configs/`

### Переключение транспортов

Если текущий транспорт работает нестабильно:

```bash
./scripts/switch-transport.sh
```

Выберите новый транспорт, скрипт автоматически:
- Создаст резервную копию текущей конфигурации
- Применит новую конфигурацию
- Перезапустит Xray
- Обновит информацию о конфигурации

### Мониторинг

#### Просмотр логов
```bash
# Логи Xray
journalctl -u xray -f

# Логи мониторинга
tail -f /var/log/xray-monitor.log

# Алерты
tail -f /var/log/xray-alerts.log
```

#### Просмотр метрик
```bash
# Метрики за текущий день
cat /var/log/xray-metrics/metrics-$(date +%Y%m%d).json

# Все метрики
ls -lh /var/log/xray-metrics/
```

#### Генерация отчета
```bash
./monitoring/generate-report.sh
```

Отчет будет сохранен в `/root/xray-reports/` и покажет:
- Uptime за период
- Использование ресурсов
- Сравнение с целевыми метриками
- Рекомендации по оптимизации

### Управление Xray

```bash
# Статус сервиса
systemctl status xray

# Перезапуск
systemctl restart xray

# Остановка
systemctl stop xray

# Запуск
systemctl start xray

# Проверка конфигурации
xray -test -config /usr/local/etc/xray/config.json

# Просмотр версии
xray version
```

---

## Troubleshooting

### Автоматическая диагностика
```bash
./scripts/troubleshoot.sh
```

Скрипт проверит:
1. ✅ Статус сервиса Xray
2. ✅ Валидность конфигурации
3. ✅ Прослушивание порта 443
4. ✅ TLS сертификаты и их срок действия
5. ✅ DNS запись и Cloudflare прокси
6. ✅ Firewall настройки
7. ✅ Ошибки в логах
8. ✅ Использование ресурсов
9. ✅ TLS подключение
10. ✅ Настройки Cloudflare

### Типичные проблемы

#### Xray не запускается
```bash
# Проверьте логи
journalctl -u xray -n 50

# Проверьте конфигурацию
xray -test -config /usr/local/etc/xray/config.json

# Проверьте сертификаты
ls -l /etc/letsencrypt/live/YOUR_DOMAIN/
```

#### Клиент не подключается
1. Проверьте DNS: `dig +short api.yourdomain.com` (должен быть IP Cloudflare)
2. Проверьте Cloudflare настройки (gRPC/WS включены?)
3. Проверьте SSL mode: Full (strict)
4. Проверьте UUID в клиентской конфигурации
5. Проверьте firewall: `ufw status`

#### Медленная скорость
1. Попробуйте другой транспорт: `./scripts/switch-transport.sh`
2. Проверьте маршрутизацию Cloudflare (может быть неоптимальной)
3. Проверьте загрузку сервера: `htop`

#### Периодические разрывы
1. Проверьте логи на ошибки: `journalctl -u xray | grep -i error`
2. Увеличьте timeout в конфигурации
3. Проверьте стабильность сетевого соединения

---

## Метрики успеха

Согласно техническому заданию, проект считается успешным при достижении:

| Метрика | Целевое значение | Как проверить |
|---------|------------------|---------------|
| Время до блокировки | >60 дней | Отслеживать календарно |
| Uptime | >95% | `./monitoring/generate-report.sh` |
| Латентность | <150ms overhead | ping + curl timing |
| Throughput | >50% от прямого | iperf3 через туннель |

### Отслеживание прогресса

```bash
# Генерация еженедельного отчета
./monitoring/generate-report.sh

# Отчет покажет:
# - Текущий uptime
# - Дней в работе
# - Статус достижения целей
# - Рекомендации
```

---

## Фазы тестирования

### Фаза 1: Базовое развертывание (неделя 1)
- [x] Настроить сервер с gRPC
- [x] Настроить Cloudflare
- [ ] Подключить тестовых пользователей
- [ ] Проверить базовую функциональность

### Фаза 2: Расширенное тестирование (недели 2-4)
- [ ] Мониторить стабильность 24/7
- [ ] Собирать статистику использования
- [ ] Протестировать WebSocket как альтернативу

### Фаза 3: Долгосрочный мониторинг (недели 5-8)
- [ ] Продолжить мониторинг блокировок
- [ ] Сравнить с предыдущим решением (~30 дней)
- [ ] Оценить деградацию производительности

### Фаза 4: Масштабирование (при успехе)
- [ ] Развернуть персональные субдомены
- [ ] Автоматизировать создание конфигураций
- [ ] Настроить ротацию субдоменов

---

## Безопасность

### Рекомендации
- ✅ Используйте сильные UUID (не предсказуемые)
- ✅ Регулярно обновляйте Xray-core
- ✅ Не логируйте клиентские IP в production
- ✅ Используйте firewall (UFW)
- ✅ Мониторьте аномальный трафик

### Настройка firewall
```bash
ufw default deny incoming
ufw default allow outgoing
ufw allow 22/tcp
ufw allow 443/tcp
ufw enable
```

---

## Обновление

### Обновление Xray
```bash
# Остановить сервис
systemctl stop xray

# Обновить Xray
bash -c "$(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)" @ install

# Проверить конфигурацию
xray -test -config /usr/local/etc/xray/config.json

# Запустить сервис
systemctl start xray
```

### Обновление сертификатов

#### Caddy (автоматически)
Caddy автоматически обновляет сертификаты. Проверка:
```bash
systemctl status caddy
```

#### Certbot (вручную)
```bash
certbot renew
systemctl restart xray
```

---

## FAQ

**Q: Какой транспорт лучше выбрать?**
A: Рекомендуется **gRPC** для Cloudflare, так как он оптимизирован для HTTP/2 и показывает лучшую производительность.

**Q: Можно ли использовать бесплатный план Cloudflare?**
A: Да, бесплатного плана достаточно для всех функций, необходимых для Xray.

**Q: Как часто нужно проверять метрики?**
A: Автоматический мониторинг настроен на каждые 5 минут. Отчеты генерируются еженедельно.

**Q: Что делать если сервер заблокировали раньше 60 дней?**
A:
1. Проверьте настройки Cloudflare
2. Попробуйте другой транспорт
3. Рассмотрите использование нескольких субдоменов
4. Проанализируйте паттерны использования

**Q: Можно ли использовать несколько транспортов одновременно?**
A: Технически да, но рекомендуется использовать один транспорт на один домен для упрощения отладки.

---

## Поддержка и контакты

### Документация
- Xray-core: https://xtls.github.io/
- Cloudflare gRPC: https://developers.cloudflare.com/support/grpc/
- V2Ray транспорты: https://www.v2fly.org/config/transport.html

### Сообщества
- Telegram: @ProjectXray
- GitHub Issues: https://github.com/XTLS/Xray-core/issues

---

## Лицензия

Этот проект предназначен для образовательных целей и тестирования антицензурных технологий.

---

## Changelog

### v1.0 (2025-11-09)
- Первый релиз
- Поддержка gRPC, WebSocket, HTTP/2 транспортов
- Автоматическая установка и настройка
- Система мониторинга и отчетности
- Диагностический скрипт
- Полная документация

---

**Примечание**: Документ предназначен для работы в связке с техническим заданием ([xray-tz.md](../xray-tz.md)). Перед началом работы ознакомьтесь с полным ТЗ.
